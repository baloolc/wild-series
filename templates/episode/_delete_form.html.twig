<form method="post" action="{{ path('episode_delete', {'slug': episode.slug}) }}" onsubmit="return confirm('voulez-vous suprimmer cette épisode ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ episode.slug) }}">
    <button class="btn btn-danger">Delete</button>
</form>
