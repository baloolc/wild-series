{% extends 'base.html.twig' %}

{% block title %}Episode
{% endblock %}

{% block body %}
	<h1>Episode</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ episode.id }}</td>
			</tr>
			<tr>
				<th>Title</th>
				<td>{{ episode.title }}</td>
			</tr>
			<tr>
				<th>Number</th>
				<td>{{ episode.number }}</td>
			</tr>
			<tr>
				<th>Synopsis</th>
				<td>{{ episode.synopsis }}</td>
			</tr>
		</tbody>
	</table>
	<div class="d-flex justify-content-center gap-3">
		<a class="btn btn-dark text-light" href="{{ path('episode_index') }}">Retour à la liste</a>
		<a class="btn btn-success" href="{{ path('app_episode_edit', {slug: episode.slug}) }}">Editer</a>
		{{ include('episode/_delete_form.html.twig') }}
	</div>
{% endblock %}
