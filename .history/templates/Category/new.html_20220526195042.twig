{% extends 'base.html.twig' %}

{% block body %}
<div class="ms-4 mt-4">
	{{ form_start(form) }}
    div.
	{{ form_widget(form) }}
	<button type="submit" class="btn btn-button my-3 bg-primary">Ajouter</button>
	{{ form_end(form) }}
</div>
{% endblock %}
