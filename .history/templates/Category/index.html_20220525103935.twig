{% extends 'base.html.twig' %}

{% block body %}
    <h1>Toutes les catégories:
		</h1>
        {% for category in categories %}
			<div>
				<h2>
					{{ category.name }}
				</h2>
				<a href="{{ path('category_show') }}">Voir les séries</a>
			</div>
		{% else %}
			Aucune catégori trouvée.
			<a href="{{ path('app_index') }}">
				Retour à l'accueil
			</a>
		{% endfor %}
{% endblock %}