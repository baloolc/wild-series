{% extends 'base.html.twig' %}

{% block body %}
<div class="ms-4 mt-4">
	{{ form_start(form) }}
    <div class="d-flex flex-column mb-2">
	{{ form_widget(form) }}
    </div>
	<button type="submit" class="btn btn-button my-3 bg-primary">Ajouter</button>
	{{ form_end(form) }}
</div>
<div>
<a href="{{ path('category_index') }}">
				Retour vers les catégories
			</a>
{% endblock %}
