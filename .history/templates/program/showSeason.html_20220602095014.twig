{% extends 'base.html.twig' %}


{% block title %}Saisons #{{ program.id }}
{% endblock %}


{% block body %}
	<div class="media ms-4">
		<img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
		<div class="media-body">
			<h1 class="mt-0">{{ program.title }}</h1>
			<h2>Saison: {{ season.id }}</h2>
                <p>{{ season.year }}</p>
                <p>{{ season.description }}</p>
            <h2>Liste des épisodes:</h2>
            <ul>
            {% for episode in episodes %}
             <li>Episode: {{ episode.number}}</li>
            <a href="{{ path('program_episode_show', { sulg: program.slug, season: season.id, slug: episode.sulg }) }}"><li>Titre: {{ episode.title}}</li></a>
            {% endfor %}
            </ul>
		</div>
        <div>
        <a href="{{ path('program_show', { slug: program.id }) }}">
		Retour vers {{ program.title }}.
	</a>
    </div>
	</div>
{% endblock %}